package com.app.gymnerd.riomale;

import org.springframework.data.jpa.repository.JpaRepository;

public interface RiomaleRepository extends JpaRepository<Riomale, Long> {
    //public Riomale findById(Long id);
}
